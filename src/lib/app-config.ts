// Static app branding. The source CRM's admin-configurable Setting/brandName
// system was dropped in Milestone A (no AI-provider-key settings page needed
// in v1) — if per-teacher branding is ever wanted, reintroduce it then.
export const APP_NAME = "Class Copilot";
